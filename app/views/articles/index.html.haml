%h2 Articles Index

= link_to 'New Article', new_article_path

%table
  %thead
    %tr
      %th ID

  %tbody
    - @articles.each do
      %tr
        %td= article.id

    - if @articles.empty?
      %tr
        %td No Articles to show.
